<header class='hero {{if(options.danger)}}is-danger{{#else}}is-dark{{/if}}'>
	<div class='hero-body'>
		<div class='container'>
			<h1 class='title is-3'>{{pageTitle}}</h1>
			{{if(options.description)}}
				<h2 class='subtitle is-5'>{{description}}</h2>
			{{/if}}
		</div>
	</div>
</header>
